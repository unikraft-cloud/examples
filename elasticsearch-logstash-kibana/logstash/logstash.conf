input {
    beats {
        port => 5044
    }
}

output {
    elasticsearch {
        hosts => "http://elasticsearch.internal:9200"
        index => "filebeat-%{+YYYY.MM.DD}"
        user => "elastic"
        password => "elastic_password123"
    }
}