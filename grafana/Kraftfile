spec: v0.7

runtime: test/base-compat

targets:
  - kraftcloud/arm64

rootfs:
  source: ./Dockerfile
  format: erofs

cmd: ["/usr/share/grafana/bin/grafana", "server", "-homepath", "/usr/share/grafana/"]
