services:
  minecraft:
    container_name: minecraft
    build:
      context: .
    ports:
      - "25565:25565"
    environment:
      EULA: "TRUE"
    mem_limit: 2G
    volumes:
      - data:/data
volumes:
  data:
    driver_opts:
      size: 5G  # this may be increased
