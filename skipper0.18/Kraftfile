spec: v0.7

runtime: test/base-compat

targets:
  - kraftcloud/arm64

rootfs:
  source: ./Dockerfile
  format: erofs

cmd: ["/usr/bin/skipper", "-address", ":9090", "-routes-file", "/etc/skipper/example.eskip"]
