spec: v0.7

runtime: test/base-compat

targets:
  - kraftcloud/arm64

rootfs:
  source: ./Dockerfile
  format: erofs

cmd: [ "/app/code-server/bin/code-server", "--host", "0.0.0.0", "--port", "8443", "--auth", "password" ]
